<div class="container-fluid">
  <div class="row">
    <div class="col-sm-6">
      <h3>Demo</h3>
      {{! BEGIN-SNIPPET infinite-scroll-example }}
      <div class="table-wrapper dark">
        <VerticalCollection
          @items={{@model.data.numbers}}
          @estimateHeight={{90}}
          @staticHeight={{true}}
          @bufferSize={{5}}
          @firstReached={{action "loadAbove"}}
          @lastReached={{action "loadBelow"}}
          as |item index|
        >
          <NumberSlide @item={{item}} @index={{index}} />
        </VerticalCollection>
      </div>
      {{! END-SNIPPET }}
    </div>
    <div class="col-sm-6">
      <h3>Infinite Scroll</h3>
      <p>
        The Vertical Collection can be used to quickly build a robust infinite
        scroll.
      </p>
      <p>
        Your infinite scroll can be bi-directional, loading new content above or
        below.
      </p>
      <p>
        Combined with `idForFirstItem`, this makes it very easy to load a user
        in-media-res: ideal for caching their position in a long feed for when
        they return.
      </p>
      <h4>Code for Demo</h4>
      <CodeSnippet @name="infinite-scroll-example" />
    </div>
  </div>
</div>